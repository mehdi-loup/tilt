# Vercel's @vercel/python builder does not support `-r <path>` includes, so
# the function's deps are listed directly. Keep in sync with the root
# requirements.txt (used by scripts/ for local preflight).
wayfinder-paths==0.11.0
httpx>=0.27.0
fastapi>=0.115
uvicorn>=0.30
asyncpg>=0.29
pyjwt[crypto]>=2.8  # verify Privy access tokens (ES256) for /wallet/withdraw
